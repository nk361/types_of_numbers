#pragma once
#include "stdafx.h"

class matrix
{
	//template type
	//array/vector/list of dimensions
	std::vector<int> dimensions_;
	//maybe make an array of ints and at each spot I can add in a loop another array
	//maybe a loop for each number in dimensions
	//then a loop that treats the dimension index as a max length
	//like for(int j = 0; j < dimensions_[i]; j++)
	//with the creation of a vector/array/list of maybe template type values
};